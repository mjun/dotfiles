# Manjaro i3wm with Qtile settings

Install Qtile and additional packages and restore settings from this repo

```bash
pacman -S qtile geany firefox
```

Qtile `config.py` closely mimics i3wm workspace behaviour.
